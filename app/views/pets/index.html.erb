<div class="container">
<p id="notice"><%= notice %></p>

<h1>Pets</h1>
  <div class="contenido">
    <table class="table">
      <thead>
        <tr>
          <th>Imagen</th>
          <th>Descripcion</th>
          <th>Dirección</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @pets.each do |pet| %>
          <tr>
            <td><%= image_tag pet.img.url(:thumb) %></td>
            <td><%= pet.description %></td>
            <td><%= pet.address %></td>
            <td><%= link_to 'Show', pet %></td>
            <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
            <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<br>

<%= link_to 'New Pet', new_pet_path %>
